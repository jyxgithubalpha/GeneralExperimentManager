# CatBoost training configuration
_target_: gem.method.training_dataclasses.TrainConfig

# Training parameters
params:
  depth: 6
  learning_rate: 0.05
  l2_leaf_reg: 3.0
  verbose: 0

num_boost_round: 100
early_stopping_rounds: 10
feval_names: ["pearsonr_ic"]
objective_name: "regression"
seed: 42
verbose_eval: 10
